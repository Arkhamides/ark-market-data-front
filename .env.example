# Anthropic (server-only)
PRIVATE_ANTHROPIC_API_KEY=

# MCP server (server-only)
MCP_SERVER_URL=http://localhost:3001/mcp

# WebSocket server (exposed to browser, must be PUBLIC_)
PUBLIC_WS_URL=ws://127.0.0.1:9002

# Supabase (exposed to browser, must be PUBLIC_)
PUBLIC_SUPABASE_HOST=
PUBLIC_SUPABASE_ANON_KEY=
